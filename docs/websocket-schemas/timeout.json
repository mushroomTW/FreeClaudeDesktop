{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Companion Timeout/Connection Error",
  "description": "Schema representing a client-side timeout or connection failure event",
  "type": "object",
  "properties": {
    "requestId": {
      "type": "string",
      "description": "The ID of the request that timed out"
    },
    "error": {
      "type": "string",
      "const": "timeout",
      "description": "The error type, constant value 'timeout'"
    },
    "message": {
      "type": "string",
      "description": "Error description (e.g., 'Request timed out after 5 seconds' or 'Connection lost')"
    }
  },
  "required": ["requestId", "error", "message"]
}
