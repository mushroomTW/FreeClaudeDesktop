# 擴充與本地技能功能介紹 (Extensions & Skills)

本專案（FreeClaudeDesktop）整合了多項針對 Claude Desktop 與大型語言模型（LLM）互動時的**效能優化與本地工具攔截機制**。這些功能不僅能顯著提升對話的啟動與回應速度，還能有效節省 API Token 的消耗。

以下為各項功能的詳細介紹與運作原理：

---

## 1. 配額檢查攔截 (Quota Mock)

* **功能描述**：攔截 `max_tokens=1` 且內容包含 `"quota"` 的測試探針請求。
* **運作原理**：
  * Claude Desktop 在啟動、網路重連或進行特定操作時，會自動向伺服器發送一個超短的測試請求（探針），用以檢測目前 API 金鑰的配額與可用性。
  * 這些探針請求不需要真正的 LLM 回答。本專案會自動在本地端識別此特徵，並直接回傳預設的「配額檢查通過。」響應。
* **優勢**：
  * **節省 Token**：避免無謂的 API 呼叫，省下 API 費用。
  * **降低延遲**：探針檢測在本地毫秒級完成，加速 Claude Desktop 介面載入。

---

## 2. 命令前綴快速檢測 (Prefix Detection)

* **功能描述**：在本地端直接解析 shell 命令的前綴，避免每次執行都呼叫 LLM 進行意圖分析。
* **運作原理**：
  * 當您在 Claude Desktop 執行指令或進行系統操作時，系統通常會向 LLM 詢問該如何下達或處理特定前綴的 shell 指令。
  * 啟用此功能後，Proxy 伺服器會使用內建的靜態前綴解析邏輯，在本地直接識別常見的開發與作業系統命令（如 `git`、`cargo`、`docker` 等），並在本地生成對應引導。
* **優勢**：
  * 大幅縮短開發流程指令的等待時間，提供近乎即時的本機指令反饋。

---

## 3. 跳過對話標題生成

* **功能描述**：開啟新對話時，跳過背景生成標題的 LLM 呼叫，直接回傳固定標題 `"Conversation"`。
* **運作原理**：
  * 官方的 Claude Desktop 在使用者送出新對話的第一句話後，會在背景發起一個隱藏的 API 請求，要求 LLM 總結該對話並返回一個簡短的標題。
  * 此功能直接攔截該標題生成請求，並在本地立刻返回固定標題 `"Conversation"`。
* **優勢**：
  * **極速開啟**：新對話建立後，不會因為背景生成標題的延遲而卡頓。
  * **省下 Token**：省去一次完整的 LLM 總結呼叫。

---

## 4. 跳過建議提問模式

* **功能描述**：跳過向 LLM 獲取建議下一步提問的請求，直接回傳空建議。
* **運作原理**：
  * Claude Desktop 有時會嘗試向模型獲取後續的推薦問題。此功能直接在 Proxy 端攔截這類請求，並快速回傳空結果。
* **優勢**：
  * 減少不必要的背景 API 網路請求與資源消耗。

---

## 5. 本機檔案路徑提取

* **功能描述**：由命令的輸出中，在本地自動分析並提取出檔案路徑，不依賴 LLM 解析。
* **運作原理**：
  * 當本機執行命令返回大量日誌，且需要提取相關檔案路徑以供編輯或讀取時，此功能會使用本地的高效正則表達式與分析演算法直接完成路徑提取。
* **優勢**：
  * 解析速度極快，且不需向遠端 API 發送大量的日誌文字，保護隱私之餘亦節省費用。

---

## 6. Web 網頁存取工具

* **功能描述**：允許在本地直接執行 `web_search` 與 `web_fetch` 等網頁內容抓取工具。
* **運作原理**：
  * 當 Claude 嘗試調用 MCP 或內建工具去搜尋網絡或獲取特定 URL 的網頁內容時，本機 Proxy 伺服器會直接扮演客戶端，在本地執行 HTTP 抓取並解析 HTML 轉為 Markdown 文字，最後將結果餵給 LLM。
  * **Web Fetch 允許 URL Schemes**：限制本地網頁存取工具僅能存取符合指定協議（如 `http,https`）的連結。
  * **允許 web_fetch 存取私有網路 (Private Networks)**：若關閉，將禁止工具存取本地端（`localhost`、`127.0.0.1`）或區域網路（如 `192.168.*.*`），有效防止 SSRF 安全漏洞。
* **優勢**：
  * 不需要額外配置複雜的 MCP Server，直接賦予 Claude 上網獲取即時資料的能力。
